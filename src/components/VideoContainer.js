import React from 'react';
import DEFAULT_PATH from '../assets/Video/video_onboarding.mp4';
const VideoContainer = ({ data }) => {
  return <div className='video-content'>{data}</div>;
};

export default VideoContainer;
